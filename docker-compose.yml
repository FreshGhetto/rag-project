services:
  rag:
    build: .
    container_name: rag_app
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./chroma_db:/app/chroma_db
    ports:
      - "8501:8501"
    tty: true
    stdin_open: true
